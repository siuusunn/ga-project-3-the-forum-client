import { useNavigate } from "react-router-dom";


  
export default function PostCard({ addedBy, text, timestamps }) {
    const navigate = useNavigate();
    // const navigateToPost = () => navigate(`/posts/${id}`);

    return <p>this is our PostCard</p>
        
      
    }

   